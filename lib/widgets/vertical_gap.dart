import 'package:flutter/material.dart';
import 'package:phonechakra/utils/size.dart';

class VGap extends StatelessWidget {
  const VGap({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: AppSize.p4,
    );
  }
}
